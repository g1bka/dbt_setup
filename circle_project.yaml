name: "gz-dbt-repository"
version: "1.0.0"
config-version: 2

#profile: "default"

models:
  marts:
    materialized: table
    finance:
      schema: finance

packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1
